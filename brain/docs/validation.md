# 確認結果と限界

この文書は公開用コピーの確認範囲を示します。元の個人環境でのテスト件数やUATを、本公開版での結果へ転記しません。

## 再現手順

READMEの6コマンドで、隔離回帰テスト・配置・完成記録・NOW同期・Markdown参照を確認します。
同梱テストは一時フォルダに架空データを作成し、実際の利用者のMemoryを変更しません。
Windowsでシンボリックリンクが許可されない場合、対応テストはSKIPになります。PASS件数に含めないでください。
2026-09-15、Windows / Python 3.12.10で実行。46テスト中45成功、シンボリックリンク権限を必要とする1件はSKIPでした。失敗0件。
初回に空のGit環境でNOW生成が失敗したため修正し、同じ手順を含む全テストを再実行して上記結果を確認しました。
配置検査は逸脱0件、Markdown参照・Project metadata検査は不備0件、NOWと案件案内の同期を確認しました。
完成記録は1件、形式・参照不備0件、partial/gapsが1件。標準モードは終了0、strictは想定どおり終了3です。
検査対象はこの文書とともに収録される公開用コピーです。外部の個人用環境への参照や依存はありません。

公開後、[GitHub Actions](https://github.com/dominantrs-glitch/nexus-core/actions/runs/34882147190)でf8b0781f16a2964039d72196ff2dc4083a354fa9のLinux上の自動検査が成功しました。
ログインしていないブラウザで、公開READMEとファイル一覧を閲覧できることも確認しました。

## まだ確認していないこと

- 第三者がリポジトリだけを読んで導入・運用できること。
- 別のAIが正しく取得・記録・再開すること。環境ごとの実試験が必要。
- すべての記録漏れ、すべての秘密情報、説明の真偽の自動検出。
- このコピーのmacOSでの実行結果。Linuxの確認は上記CIの範囲。
- 導入による作業時間削減や判断品質の改善。

Markdownリンク検査はローカルの参照先ファイルを対象とします。見出し内アンカーと外部サイトの可用性は別です。
完成記録のstrict検査もメタデータと形式の確認であり、本文の意味や全履歴の網羅性を検証しません。
